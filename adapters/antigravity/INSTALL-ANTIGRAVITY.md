# HDF Blog Editor for Antigravity

This package places the HDF Blog Editor in `.agent/skills/hdf-blog-editor/` and adds the `/review-hdf-blog` workspace workflow under `.agents/workflows/`.

Extract the package into the workspace root, refresh Antigravity, then type:

> I would like to review the next blog.

You may also invoke `/review-hdf-blog` directly.
